export default function LanguageOptions() {
	return (
		<select name="languages" className="bg-black text-white">
			<option value="english">English</option>
			<option value="french">French</option>
			<option value="german">German</option>
		</select>
	);
}
